version: "3.9"

services:
  react:
    build:
      context: ../frontend/certstack
      dockerfile: ../../infra/Dockerfile.react
    ports:
      - "5173:5173"
    volumes:
      - ../frontend/certstack:/app
      - /app/node_modules
    command: npm run dev